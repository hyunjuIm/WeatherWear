<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WeatherWear" parent="Theme.MaterialComponents.Light.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_700</item>
        <item name="colorPrimaryVariant">@color/blue_500</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="android:windowBackground">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- 폰트 설정 -->
        <item name="android:fontFamily">@font/notosans_kr_regular</item>
        <item name="android:letterSpacing">-0.02</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:includeFontPadding">false</item>
        <item name="colorAccent">@color/gray_500</item>

    </style>

    <!-- 홈 화면 테마 -->
    <style name="Theme.WeatherWear.Home">
        <item name="android:statusBarColor">@color/sky_100</item>
        <item name="android:windowBackground">@drawable/bg_gradient_blue_sky</item>
    </style>

</resources>